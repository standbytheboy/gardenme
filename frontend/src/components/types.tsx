// Crie um arquivo separado para essa interface, por exemplo, `types.ts`
export interface Plant {
  id: number;
  name: string;
  price: number;
  imageSrc: string;
  description: string;
  rating: number;
  quantity: number
}